{
  "mcpServers": {
    "ponder": {
      "command": "bun",
      "args": ["run", "--cwd", "${CLAUDE_PLUGIN_ROOT}", "--shell=bun", "--silent", "start"],
      "env": {
        "PONDER_URL": "http://localhost:9077",
        "PONDER_AGENT_ID": "claude"
      }
    }
  }
}
